{% extends 'layouts/base.html.twig' %}

{% block title %} Change Password {% endblock title %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1 class="mb-5 mt-5 h3 text-center">Change Account's Password</h1>
            {{  form_start(form)  }}
                {{  form_widget(form)  }}
                <button type="submit" class="btn btn-primary btn-block btn-lg" formnovalidate>Update Password</button>
            {{  form_end(form)  }}
        </div>
    </div>
{% endblock body %}